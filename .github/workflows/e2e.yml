#name: e2e
#
#on:
#  pull_request:
#    branches: ['main']
#
#jobs:
#  e2e:
#    runs-on: self-hosted
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Cypress run
#        uses: cypress-io/github-action@v6
#        with:
#          build: npm run build
#          start: npm run start:autotests
#          browser: chrome
#          wait-on: ${{ vars.SERVER_SIDE_URL }}
#        env:
#          SERVER_SIDE_URL: '${{ vars.SERVER_SIDE_URL }}/api'
#          BACKAPI_URL: ${{ vars.BACKAPI_URL }}
#
#      - name: Upload screenshots
#        uses: actions/upload-artifact@v4
#        if: failure()
#        with:
#          name: cypress-screenshots
#          path: cypress/screenshots
